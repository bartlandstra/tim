---
const shows = [
  { date: '2026-05-07', venue: 'TivoliVredenburg', city: 'Utrecht', act: 'Sven & Matthijs', status: 'tickets', url: 'https://www.tivolivredenburg.nl/agenda/94164511/sven-matthijs-07-05-2026' },
  { date: '2026-05-10', venue: 'Paradiso', city: 'Amsterdam', act: 'Sven Hammond Big Band', status: 'tickets', url: 'https://www.paradiso.nl/programma/sven-hammond/118662' },
  { date: '2026-06-07', venue: 'Theater Walhalla', city: 'Rotterdam', act: 'Na de Stilte (met Beatrice van der Poel)', status: 'tickets', url: 'https://www.theaterwalhalla.nl/voorstelling/beatrice-van-der-poel-na-de-stilte/' },
  { date: '2026-06-21', venue: 'Bimhuis', city: 'Amsterdam', act: 'Tim Eijmaal Trio', status: 'soon', url: '' },
  { date: '2026-07-12', venue: 'North Sea Jazz Festival', city: 'Rotterdam', act: 'New Cool Collective ft. Tim Eijmaal', status: 'tickets', url: 'https://www.northseajazz.com/en/tickets' },
  { date: '2026-09-05', venue: 'De Roode Bioscoop', city: 'Amsterdam', act: 'Na de Stilte (met Beatrice van der Poel)', status: 'tickets', url: 'https://www.roodebioscoop.nl/beatrice-van-der-poel-na-de-stilte' },
];

function formatDate(dateStr: string): string {
  const d = new Date(dateStr + 'T12:00:00');
  const months = ['jan', 'feb', 'mrt', 'apr', 'mei', 'jun', 'jul', 'aug', 'sep', 'okt', 'nov', 'dec'];
  const days = ['zo', 'ma', 'di', 'wo', 'do', 'vr', 'za'];
  return `${days[d.getDay()]} ${d.getDate()} ${months[d.getMonth()]}`;
}
---

<section id="tourdates" class="py-24 md:py-32 px-6 md:px-12">
  <div class="max-w-6xl mx-auto">
    <div class="section-line mb-6"></div>
    <h2 class="text-3xl md:text-4xl font-bold tracking-tight mb-4">
      Tour <span style="color: var(--color-accent);">Dates</span>
    </h2>
    <p class="text-lg mb-12 max-w-xl" style="color: var(--color-text-muted);">
      Aankomende optredens en concerten.
    </p>

    <div class="space-y-1">
      {shows.map(show => (
        <div class="group flex flex-col sm:flex-row sm:items-center gap-2 sm:gap-0 p-5 border-b transition-colors duration-300 hover:bg-[var(--color-surface)]" style="border-color: var(--color-surface);">
          <!-- Date -->
          <div class="sm:w-32 shrink-0">
            <span class="text-sm font-bold uppercase tracking-wide" style="color: var(--color-accent);">
              {formatDate(show.date)}
            </span>
          </div>

          <!-- Venue & City -->
          <div class="flex-1 min-w-0">
            <div class="font-semibold text-white">{show.venue}</div>
            <div class="text-sm" style="color: var(--color-text-muted);">{show.city}</div>
          </div>

          <!-- Act -->
          <div class="flex-1 min-w-0 hidden md:block">
            <div class="text-sm" style="color: var(--color-text-muted);">{show.act}</div>
          </div>

          <!-- Status -->
          <div class="sm:w-32 shrink-0 sm:text-right">
            {show.status === 'tickets' && show.url ? (
              <a href={show.url} target="_blank" rel="noopener" class="inline-block text-xs font-medium uppercase tracking-wide px-4 py-2 transition-all duration-300 hover:scale-105" style="background: var(--color-accent); color: var(--color-bg);">
                Tickets
              </a>
            ) : (
              <span class="text-xs font-medium uppercase tracking-wide px-4 py-2 border" style="border-color: var(--color-accent-dim); color: var(--color-accent);">
                Binnenkort
              </span>
            )}
          </div>
        </div>
      ))}
    </div>

    <div class="mt-12 text-center">
      <p class="text-sm" style="color: var(--color-text-muted);">
        Wil je Tim boeken voor jouw evenement?
        <a href="#contact" class="underline transition-colors duration-300 hover:text-[var(--color-accent)]" style="color: var(--color-accent);">Neem contact op</a>
      </p>
    </div>
  </div>
</section>
